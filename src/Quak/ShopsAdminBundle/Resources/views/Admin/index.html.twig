{% extends '::base.html.twig' %}

{% block body %}
    <div>
        <h1>Administrators</h1>
        <ul>
            {% for user in users.ROLE_ADMIN %}
            <li>{{ user.name }}</li>
            {% endfor %}
        </ul>
    </div>
    <div>
        <h1>Shops</h1>
        <ul>
            {% for user in users.ROLE_SHOP %}
            <li>{{ user.name }}</li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}